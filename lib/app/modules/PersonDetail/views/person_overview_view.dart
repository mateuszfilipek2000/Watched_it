import 'package:flutter/material.dart';

class PersonOverviewView extends StatelessWidget {
  const PersonOverviewView({Key? key, required this.tag}) : super(key: key);

  final String tag;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
